# Tested with Python 3.11 and these exact versions; >= bounds are used here
# instead of hard pins so this also installs cleanly on Binder/Colab, which
# may run a different Python version.
pandas>=3.0.5
numpy>=2.4.6
matplotlib>=3.11.1
seaborn>=0.13.2
scipy>=1.17.1
